apiVersion: v1
kind: Service
metadata:
  name: {{ include "light.fullname" . }}
  labels:
    {{- include "light.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "light.selectorLabels" . | nindent 4 }}
