apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "full.fullname" . }}-test-connection"
  labels:
    {{- include "full.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "full.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
